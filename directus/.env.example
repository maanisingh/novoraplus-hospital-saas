# Directus Configuration for Railway
# These should be set as Railway environment variables

# Security Keys (generate unique ones for production!)
KEY=hospital-saas-key-change-in-production
SECRET=hospital-saas-secret-change-in-production

# Database (Railway will auto-inject DATABASE_URL if using Railway PostgreSQL)
# If using DATABASE_URL from Railway, Directus will parse it automatically
DB_CLIENT=pg
# Or use individual settings:
# DB_HOST=your-postgres-host
# DB_PORT=5432
# DB_DATABASE=hospital_saas
# DB_USER=directus
# DB_PASSWORD=your-secure-password

# Redis Cache (optional - Railway Redis service)
# CACHE_ENABLED=true
# CACHE_STORE=redis
# REDIS=redis://your-redis-url:6379

# Admin Account (set strong credentials!)
ADMIN_EMAIL=admin@yourhospital.com
ADMIN_PASSWORD=ChangeThisPassword123!

# CORS
CORS_ENABLED=true
CORS_ORIGIN=true

# File Storage
STORAGE_LOCATIONS=local
STORAGE_LOCAL_DRIVER=local
STORAGE_LOCAL_ROOT=/directus/uploads

# Public URL (set to your Railway domain)
PUBLIC_URL=https://your-directus-domain.railway.app

# Token TTL
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
